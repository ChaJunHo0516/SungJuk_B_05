```mermaid
sequenceDiagram
    autonumber
    actor 교수
    participant UI as Add_Sungjuk_UI <<boundary>>
    participant Score as 성적
    participant Prof as 교수

    교수->>UI: 성적입력 요청(교수id, 학생id, 자바, DB, 보안)
    activate UI

    UI->>Score: 성적입력(교수id, 학생id, 자바, DB, 보안)
    activate Score

    Score->>Prof: 교수체크(교수id: String)
    activate Prof
    Prof-->>Score: 체크결과(boolean)
    deactivate Prof

    alt 체크결과 == true
        Note over Score: 총점 및 평균 계산
        Score-->>UI: "입력 성공 (총점, 평균 포함)" 반환
    else 체크결과 == false
        Score-->>UI: "교수 인증 실패" 반환
    end
    deactivate Score

    UI-->>교수: 처리 결과 출력
    deactivate UI
```
