```
sequenceDiagram
    autonumber
    actor 교수
    participant UI as Add_Sungjuk_UI <<boundary>>
    participant Prof as 교수
    participant Score as 성적

    교수->>UI: 성적입력 요청(교수id, 학생id, 자바, DB, 보안)
    activate UI

    UI->>Prof: 교수체크(교수id: String)
    activate Prof
    Prof-->>UI: 체크결과(boolean)
    deactivate Prof

    alt 교수id == "inha"
        UI->>Score: 성적입력(학생id: String, 자바: int, DB: int, 보안: int)
        activate Score
        Note over Score: 총점 및 평균 계산
        Score-->>UI: 입력 성공 메시지
        deactivate Score
        UI-->>교수: 처리 결과 출력 (총점, 평균 포함)
    else 교수id != "inha"
        UI-->>교수: "교수 인증 실패" 메시지 출력
    end

    deactivate UI
```
